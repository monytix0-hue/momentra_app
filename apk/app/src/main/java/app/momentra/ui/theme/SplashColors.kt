package app.momentra.ui.theme

import androidx.compose.ui.graphics.Color

/** Splash / launch mark accents (paired with [MomentraBase.brandDeep] background). */
object SplashColors {
    val ember = Color(0xFFE8621A)
    val amber = Color(0xFFF5A623)
}
