package app.momentra.ui.theme

/** Context types for Momentra — matches iOS `MomentraContext`. */
enum class MomentraContext(val displayName: String) {
    Personal("Personal"),
    Group("Group"),
    Business("Business"),
    Circle("Circle"),
}
