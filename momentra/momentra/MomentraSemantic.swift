import SwiftUI

/// Semantic roles that map to visual intent (above primitive colors).
enum MomentraSemantic {
    /// Theme kit v2.2 rule: CTA labels are always white.
    static let ctaText = Color.white
}
